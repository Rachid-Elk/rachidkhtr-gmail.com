{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div class="card bg-info text-white text-center"><h1 >Produit</h1></div></div><br><br>

    {{ form_start(edit_form) }}
    <div class="d-inline-block  col-md-6 col-xs-6 col-md-offset-3">
        {{ form_widget(edit_form) }}
         <button  class="btn btn-outline-success"  type="submit" value="Edit">
                    <i class="fa fa-edit"></i>
                    Modifier
                </button> 
    {{ form_end(edit_form) }}
        </div><br>
         <div class="d-inline-block">
            <a  class="btn btn-primary " href="{{ path('produit_index') }}">
                <i class="fa fa-table"></i>
                Liste
            </a>
        </div>
        <div class="d-inline-block ">
            {{ form_start(delete_form) }}
                <!--input type="submit" -->
                <button class="btn btn-danger" 
                onclick="return confirm('Etes vous sure pour Supprimer ?');" type="submit" value="Delete">
                <i class="fa fa-trash"></i>
                 Supprimer
                </button>
            {{ form_end(delete_form) }}
        </div>

    </div>
{% endblock %}
